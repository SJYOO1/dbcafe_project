<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.dbcafe.dao.YgoalDAO">
	<select id="sumYearSales" resultType="int" parameterType="date">
		SELECT sum(totPrice)
		FROM DETAIL d, ORD o
		WHERE d.ordNo=o.ordNo AND o.ordDt
		BETWEEN TO_DATE('{#startDt} 00:00:00', 'YYYY-MM-DD hh24:mi:ss')
		AND TO_DATE('{#endDt} 23:59:59', 'YYYY-MM-DD hh24:mi:ss')
	</select>
</mapper>