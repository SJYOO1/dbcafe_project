<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.dbcafe.dao.OrdertypeDAO">
	<select id="ordertypeSelect" resultType="OrdertypeDTO" parameterType="map">
	SELECT 
	 o.TYPENO typeNo,SUM(tt) sum 
FROM 
	ORD o ,
	(SELECT 
	d.ORDNO nno,sum(d.TOTPRICE) tt
FROM 
	DETAIL d 
GROUP by
	d.ORDNO) d2
WHERE
	o.ORDNO = nno and o.stNo = #{storeNum} AND o.ORDDT  BETWEEN TO_DATE(#{startDay}, 'YYYY-MM-DD') 
   AND TO_DATE(#{endDay}, 'YYYY-MM-DD')
GROUP BY 
	o.TYPENO
	</select>
	<select id="today" resultType="string">
	select sysdate from dual
	</select>
</mapper>